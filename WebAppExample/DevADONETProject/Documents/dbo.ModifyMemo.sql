CREATE PROCEDURE dbo.ModifyMemo
(
	@NUM	INT,
	@NAME	NVARCHAR(25),
	@EMAIL	NVARCHAR(100),
	@TITLE	NVARCHAR(150)
)
AS
BEGIN TRANSACTION
	UPDATE dbo.Memos
	SET 
		NAME = @NAME,
		EMAIL = @EMAIL,
		TITLE = @TITLE
	WHERE
		NUM = @NUM;
COMMIT TRANSACTION
GO

		